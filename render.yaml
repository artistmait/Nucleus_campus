services:
  - type: web
    name: nucleus-flask-backend
    env: python
    plan: free
    rootDir: nucleus_flask_backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
    healthCheckPath: /health
